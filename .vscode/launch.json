{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge", // <--- Changed from "chrome" to "msedge"
            "request": "launch",
            "name": "Launch Edge against file",
            "file": "${workspaceFolder}/quiz.min.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}